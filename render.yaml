services:
  - type: web
    name: 602-ops-portal
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm run start
    autoDeployTrigger: checksPass
    envVars:
      - key: NODE_VERSION
        value: 22
